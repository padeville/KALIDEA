import { FormFildInputDirective } from './form-fild-input.directive';

describe('FormFildInputDirective', () => {
  it('should create an instance', () => {
    const directive = new FormFildInputDirective();
    expect(directive).toBeTruthy();
  });
});
